const joyrideStyling = {
  options: {
    // modal arrow and background color
    arrowColor: '#fff',
    backgroundColor: '#fff',
    // page overlay color
    // overlayColor: 'rgba(4, 50, 35, 0.6)',
    //button color
    primaryColor: '#006462',
    //text color
    textColor: '#003635',
    //width of modal
    width: 500,
    //zindex of modal
  },
};

export default joyrideStyling;
